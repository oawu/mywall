$(function() {
  // $('.active_image').each (
  // var $active_image = $('.active_image');
  // $active_image.imagesLoaded (function () {
  //   $active_image.css ({'height': parseFloat ($active_image.find ('img').css ('height')) }).imgLiquid ({horizontalAlign: "top"});
  // });

});